name: Issue Triage

on:
  issues:
    types: [opened, labeled]
  pull_request:
    types: [opened, labeled]

jobs:
  triage:
    runs-on: ubuntu-latest
    steps:
    - name: Label triage
      uses: actions/labeler@v5
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
